{
  "gate": "M6 Split / Merge Reliability Architecture",
  "status": "OPEN — no decision below has been adopted by this research",
  "productionBase": "544a3baf6c7dcee02090dc90058735e377a08ea8",
  "revision": "answers Required Fix A/B/C/D — the optional-content detection envelope, artifact-wide JavaScript remnants, /Order label position, and decision-record consistency. Builds on RF-M6-R1…R6, RF-S1/S2, RF-U1/U2 and RF-V1.",
  "evidence": [
    "evidence.json",
    "evidence-browser.json",
    "baseline.md",
    "source-preservation-matrix.md",
    "structure-policy.md",
    "extract-contract.md",
    "merge-contract.md",
    "preview-memory.md",
    "budget.md",
    "limitations.md"
  ],
  "decisions": [
    {
      "id": "M6-H1",
      "question": "Applied signature policy for Extract",
      "candidates": [
        "A — hard refuse any source carrying an applied signature",
        "B — allow the unsigned derivative, but remove the signature widget and its appearance stream",
        "C — allow, with a persistent artifact-level indication that this is an unsigned derivative"
      ],
      "measured": "The cryptographic signature is removed and the appearance is not. After a legacy extract the AcroForm is absent, no signature field remains, and one widget survives carrying its /AP. Rendering the signature rectangle gives 4,325 non-white pixels out of 24,300 in the source and 4,325 out of 24,300 in the extract — pixel for pixel identical. A reader opening the derived file sees a signed-looking block on a document that carries no signature.",
      "recommended": "B",
      "why": "The previous recommendation allowed the unsigned derivative without saying anything about the appearance, which the measurement now shows is the part the reader actually sees. Removing the widget and its /AP makes the derived document honestly unsigned. C is a reasonable addition to B rather than an alternative to it; A remains correct for anyone who does not want derived copies of signed documents at all.",
      "defer": ["what a persistent artifact-level indication should be (C) — visible watermark, metadata, or both"]
    },
    {
      "id": "M6-H2",
      "question": "Applied signature policy for Merge",
      "candidates": ["A — refuse any source carrying an applied signature", "B — permit with explicit confirmation"],
      "measured": "Merging a signed source produced a 5-page ordinary file with no mention of the signature. The appearance finding in M6-H1 applies here too, and is worse: the signed document's appearance now sits among other people's content.",
      "recommended": "A",
      "why": "Unchanged by the new evidence, and strengthened by it. The asymmetry with M6-H1 is deliberate: an extract is openly a derivative of one document, a merge is a new document that would carry another's signature block.",
      "defer": []
    },
    {
      "id": "M6-H3",
      "question": "AcroForm and widget preservation",
      "candidates": [
        "S1-A — reconstruct simple /Tx only, typed refusal for everything else",
        "S1-B — AcroForm is a typed refusal in the M6 MVP, with a Form Reconstruction Sub-Spike to follow",
        "reconstruct a broader subset, once each type has fixtures and post-readback proof"
      ],
      "measured": "The previous round advertised /Tx /Btn /Ch /Sig on the strength of one type's evidence: the reconstruction only ever restored /T, /FT and /V. It read /Ff without writing it back, never touched /DV, never carried AcroForm /DR, and never checked that a button's /AS still names a state in its /AP. Each shape now has a fixture. Rebuilt: form-tx-plain, value M6-TX-VALUE preserved, 0 orphan widgets. Refused as UNSUPPORTED_FORM: form-tx-ff, form-tx-dv, form-dr, form-separate-widget, form-hierarchical, form-checkbox, form-radio, form-choice. Refused as FIELD_SPANS_SELECTION: form-field-across-pages, which reports the straddle as its primary reason and carries the other subset reasons with it. Refused as DUPLICATE_FIELD_NAMES on request: collide-a + collide-b, which rename to source1./source2. with both values intact and 0 orphan widgets when renaming is chosen.",
      "supportedSubset": ["/Tx, merged field/widget, with none of /Ff, /DV, /AA, no separate widget dictionaries, no inherited /FT or /V, and a /DA that does not name a font from AcroForm /DR"],
      "refusedWithFixtures": {
        "/Ff": "form-tx-ff — read, never restored",
        "/DV": "form-tx-dv — not restored",
        "AcroForm /DR": "form-dr — the /DA names a /DR font that is not carried",
        "separate field + widget dictionaries": "form-separate-widget",
        "inherited /FT or /V": "form-hierarchical",
        "/Btn checkbox": "form-checkbox — /AS and /AP consistency unproven",
        "/Btn radio group": "form-radio — kids hold their own appearance states",
        "/Ch": "form-choice — /Opt not restored",
        "/CO, field /AA, document JavaScript": "detected and refused, which is what makes rename-on-collision offerable at all"
      },
      "preservedCodes": ["FIELD_SPANS_SELECTION", "DUPLICATE_FIELD_NAMES", "zero orphan widgets in any successful output"],
      "recommended": "S1-A — supported reconstruction is simple /Tx only",
      "why": "This is what the committed evidence proves, and no more. Widening the subset means adding fixtures and post-readback proof for each type, not editing a list of names — which is how the previous recommendation came to claim four types on one type's evidence. Applied signatures stay governed by M6-H1 and M6-H2 rather than by generic form reconstruction.",
      "defer": [
        "/Btn checkbox and radio groups, including /AS to /AP consistency",
        "/Ch and /Opt",
        "ordinary /Sig form reconstruction",
        "/Ff and /DV restoration",
        "AcroForm /DR carrying and merging",
        "separate field and widget dictionaries",
        "hierarchical /FT and /V inheritance",
        "whether the MVP ships /Tx reconstruction at all (S1-A) or refuses AcroForm entirely (S1-B)"
      ]
    },
    {
      "id": "M6-H4",
      "question": "XFA",
      "candidates": ["refuse where the operation would drop it", "declare unsupported and name the loss"],
      "measured": "XFA present in the source is absent from the output, unreported, for both operations.",
      "recommended": "refuse, following M5's adopted refuse-if-dropped",
      "why": "Consistency across tools matters more than a marginal capability, and M5 already owns the contract.",
      "defer": []
    },
    {
      "id": "M6-H5",
      "question": "Internal destinations pointing at pages excluded from an Extract",
      "candidates": [
        "E1 — refuse the selected set, naming the pages that would break",
        "E2 — keep the set, remove or retarget the link, report the loss",
        "E3 — drop the link silently (not recommended)"
      ],
      "measured": "Today is none of these. Measured per shape: a destination to a SELECTED page still produces an orphan copy and lands outside the page tree (dest-direct-2p with both pages kept: 2 in tree, 1 orphan, 0 destinations landing in the document); the same holds for /A /GoTo /D. Cyclic destinations produce 2 orphans from a one-page selection. An article bead reaches another page with no annotation involved at all. The E2 prototype produces 0 orphan pages in all ten cases, every surviving destination lands in the output page tree, and every removed one is reported; E1 refuses before copying anything.",
      "recommended": "E2 as the default, E1 available as a stricter setting, and the invariant below adopted as a production contract",
      "invariant": "every indirect /Page object in an Extract artifact is a member of its output page tree (orphanPageCount === 0), and every surviving internal destination targets a page of that tree",
      "why": "E0 — what ships today — is worse than E3: it keeps a link that navigates nowhere a reader can reach AND ships the content of pages the user did not select. The invariant is checkable after serialization, which is where it is checked in the gate.",
      "defer": []
    },
    {
      "id": "M6-H6",
      "question": "Outlines, named destinations and page labels",
      "candidates": ["reconstruct for kept pages", "declare dropped and name the loss"],
      "measured": "All three are dropped by both operations today, even when every page is extracted. Named destinations ARE reconstructable: the E2 prototype rebuilds the entries whose target survives and reports the rest — with both pages of dest-named-2p kept, 4 destinations land in the document and nothing is lost; keeping only page 1, the surviving entry is rebuilt and 2 losses are reported by name. Outlines and page labels were NOT prototyped.",
      "recommended": "reconstruct named destinations (proven); treat outlines and page labels as reconstructable in principle but unproven here",
      "why": "Saying 'reconstruct' for all three would claim evidence that exists for one of them. Page labels are arithmetic on /Nums and outlines are a tree walk, so neither looks hard — but neither has been shown to work, and the difference between those two statements is the point of this document.",
      "defer": [
        "outline reconstruction, including hierarchies deeper than one level",
        "page-label range arithmetic across a non-contiguous selection"
      ]
    },
    {
      "id": "M6-H7",
      "question": "Extract metadata",
      "candidates": ["carry the source's metadata under M5 H12", "neutral derived metadata"],
      "measured": "Output carries only pdf-lib's /Producer /ModDate /Creator /CreationDate; /Title /Author /Subject /Keywords /Company /M6Custom /M6Indirect and the XMP packet are all lost.",
      "recommended": "carry it, under M5's H12 contract",
      "why": "Extract has exactly one source, so there is no ambiguity, and M5 already owns a tested carry-or-refuse implementation.",
      "defer": []
    },
    {
      "id": "M6-H8",
      "question": "Merge metadata",
      "candidates": [
        "M1 — first source's metadata",
        "M2 — neutral newly-derived metadata",
        "M3 — the user chooses a source",
        "M4 — synthesized provenance naming the sources"
      ],
      "measured": "Output carries pdf-lib's four keys — M2 by accident, and without saying so.",
      "recommended": "M4, with M1 available",
      "why": "A merged document is a new document and its honest metadata says so. M1 fits the common 'append appendices to this drawing' shape and should be offered rather than assumed.",
      "defer": []
    },
    {
      "id": "M6-H9a",
      "question": "Tagged documents — /StructTreeRoot",
      "candidates": ["refuse a tagged document", "strip every tagging remnant and confirm the accessibility loss", "reconstruct the structure tree"],
      "measured": "The drop is not clean. Before: {structTreeRoot: true, markInfo: true, pageStructParents: true}. After a legacy extract: {false, false, true}. The artifact keeps a page claiming membership of a structure tree it does not contain.",
      "detector": "catalog /StructTreeRoot present, or any page carrying /StructParents",
      "recommended": "strip every remnant — /StructParents included — and report the accessibility loss explicitly",
      "why": "Shipping the half-structure is not an option once it is known about: a page that claims a parent tree that is absent is a document that lies about itself. Refusing tagged documents outright would block a legitimate and common case; reconstruction is a genuine feature, not an MVP.",
      "defer": ["structure-tree reconstruction for kept pages"]
    },
    {
      "id": "M6-H9b",
      "question": "Optional content — /OCProperties",
      "candidates": [
        "carry the configuration for groups the kept pages reference, refusing anything outside the handled envelope",
        "refuse every document carrying optional content",
        "drop and confirm the visibility change"
      ],
      "measured": "Today's drop is not clean: before {ocProperties: true, pageOptionalContentProperties: true}, after {false, true} — the page's marked content still names a group the document no longer configures, so a viewer shows its own default rather than the author's. Thirteen documents now carry and match their source after the artifact is reopened, on group count, group names, ON, OFF, /D /Name, /BaseState, /D /Order structure and page /Properties resolution, all equal in every one. Eight for the group mapping: ocproperties (1 carried, 1 on), ocg-multiple (2 carried, 1 on, 1 off), ocg-two-pages-opposite (2 carried, on [M6-OCG-A], off [M6-OCG-B]), ocg-shared-across-pages (1 carried, referenced from both pages), ocg-reordered-properties (2 carried, keys written in opposite order on each page), ocg-many-pages (3 carried over 3 pages, 2 on, 1 off, one shared), ocg-d-name (/D /Name reproduced), ocg-basestate-on (/BaseState /ON reproduced). Five for the order structure: ocg-order-flat, ocg-order-nested, ocg-order-labeled-nested, ocg-order-empty (stays empty) and ocg-order-absent (stays absent). The two-page opposite-state pairing is the one an earlier positional mapping could invert. Refused with UNSUPPORTED_OPTIONAL_CONTENT: ocmd, ocmd-nested, ocg-basestate-off, ocg-order-malformed, ocg-configs, annot-oc, xobject-oc, malformed-ocproperties, ocg-order-invalid-top-label, ocg-order-invalid-mid-label, ocg-order-unused-group, ocg-missing-ocgs, ocg-missing-d, ocg-dangling-ocgs-ref, ocg-nested-form-xobject, ocg-form-properties, ocg-annotation-appearance-properties, ocg-pattern-properties, ocg-type3-properties, resource-cycle and resource-depth-exceeded. The fourteen supported fixtures still read as carryable with the resource-graph walker in place. One document with a group the walker does not reach, ocg-extgstate-smask, extracts READY: that is the measured edge of the walked scope, not a clean result.",
      "envelope": {
        "carried": "the page's own /Resources /Properties entries resolving to plain /OCG dictionaries, with a /D whose keys are within Order, ON, OFF, Name, BaseState — and which are reproduced in the artifact, not merely accepted at the door",
        "refused": "/OCMD in any form, a /VE visibility expression, a /BaseState other than /ON, any other /D key, an /Order entry that is neither a group, a nested array nor a text label, an /Order text label anywhere but the first element of a nested array, an /Order naming a group no kept page uses, any /Properties entry that is neither /OCG nor /OCMD, /OCProperties /Configs, an annotation carrying /OC, an XObject carrying /OC at any depth the walker reaches, an /OCProperties that cannot be read, an /OCProperties without /OCGs or without /D, an /OCGs entry that does not resolve or is not a group, any /Properties entry below the page's own resources — in a form XObject, an annotation appearance stream, a tiling pattern or a Type 3 font — and a resource graph deeper than 24 scopes"
      },
      "detectionEnvelope": "A contract that refuses everything outside the handled shapes is only true if the unhandled shapes can be found. The detector has been widened twice, each time with a fixture per shape. First, four attachment points that were invisible: /OCProperties /Configs (ocg-configs), an annotation's /OC (annot-oc), an XObject's /OC (xobject-oc) and an /OCProperties that cannot be walked (malformed-ocproperties). Second, the resource graph below the page, which a page-level scan never opened: a form XObject two levels down carrying /OC (ocg-nested-form-xobject), a form XObject's own /Resources /Properties (ocg-form-properties), an annotation appearance stream's own /Resources /Properties (ocg-annotation-appearance-properties), a tiling pattern's (ocg-pattern-properties) and a Type 3 font's (ocg-type3-properties). Required keys fail closed: a missing /OCGs (ocg-missing-ocgs) or /D (ocg-missing-d) is refused rather than read as an empty default, and an /OCGs entry pointing at an object that does not exist (ocg-dangling-ocgs-ref) is refused rather than read as a value. The walk is bounded: a visited set keyed on indirect references ends a cycle and counts a shared object once (resource-cycle — the group behind the cycle is reported exactly once and the depth bound is never reached), and a graph deeper than 24 scopes is refused rather than reported as clean (resource-depth-exceeded). Walked: /Annots and their /AP streams, /XObject, /Pattern, Type 3 /Font, and /Properties below the page's own scope. Not walked: /ColorSpace and /Shading, which open no content stream; and /ExtGState, which is not an attachment point but whose soft mask /G is a form XObject with resources of its own — ocg-extgstate-smask extracts READY with a group behind it, so that path is recorded as unmeasured rather than clean. Followed by the walker but exercised by no fixture: /AP state dictionaries, image XObjects below the page, and /CharProcs streams carrying their own /Resources. Support was not widened by any of this: the refusals became real.",
      "orderRule": "/Order is mapped recursively rather than flattened: a group reference becomes the mapped output reference, a nested array becomes a nested array, an absent /Order stays absent and an empty one stays empty. A text label is reproduced only where this research has shown it can be — as the first element of a nested array, titling a section. A label at the top level (ocg-order-invalid-top-label) or part-way through a nested array (ocg-order-invalid-mid-label) is refused rather than carried on the strength of being a string, and an /Order naming a group no kept page uses (ocg-order-unused-group) is refused rather than silently dropped. Proven on five carried shapes — flat, empty, absent, nested, and nested opening with a label — each compared as a structure after reopening the artifact, not as a set of groups. An earlier version collected only the references it found at any depth and rewrote a flat array, so nesting and labels were silently lost, an empty /Order was filled in and an absent one was invented; the comparison did not notice because it compared the set.",
      "mapping": "structural: (selected source page + /Properties key + source OCG ref) to (output page + same key + output OCG ref), with the resulting output refs deduplicated. Positions are never used, so a page whose keys are written in a different order, or a group referenced from two pages, still resolves. An earlier version paired by position and reset its source cursor on every output page: one kept page worked, two could swap ON for OFF.",
      "provenOnReadback": ["group count", "group names", "ON", "OFF", "/D /Name", "/BaseState", "/D /Order structure", "page /Properties resolution"],
      "detector": "catalog /OCProperties present, or optional content found by walking each kept page's resource graph — /Annots and their /AP streams, /XObject, /Pattern and Type 3 /Font scopes, recursively — with a visited set and a depth bound of 24; an unresolvable reference is a refusal, not an absence",
      "recommended": "carry within the envelope above; typed refusal outside it",
      "why": "Carrying is proven for the direct-/OCG case and the refusal is proven for /OCMD, so neither half is asserted. An /OCMD decides visibility from a set of groups and optionally a nested boolean expression; carrying a subset of that evaluation would silently change what the reader sees, which is the failure this decision exists to prevent.",
      "defer": [
        "/OCMD, with or without a /VE visibility expression — refused for now, not designed",
        "/BaseState /OFF and /Unchanged",
        "usage application dictionaries, /RBGroups and /Locked, which fall outside the handled /D keys",
        "the /ExtGState soft-mask path (/SMask /G), which the walker does not open — a group behind it is not detected (ocg-extgstate-smask)"
      ]
    },
    {
      "id": "M6-H9c",
      "question": "Document JavaScript and /OpenAction",
      "candidates": ["refuse any JavaScript-bearing source", "sanitize: remove and prove by readback", "carry"],
      "measured": "Seven sites, each with its own fixture, each found by the scanner in the source and absent from the sanitized output measured by reopening it: /Names /JavaScript, /OpenAction as a JavaScript action, annotation /A, annotation /AA, page /AA, field /AA, and JavaScript behind a /Next. Total remaining after readback across all seven: 0. A document with no JavaScript scans 0 and completes. An action reachable by two routes through the same holder is counted once — 1 action from 2 visits. Five /Next shapes then measured separately, all reaching 0 remaining: a dictionary, an array, an array whose second element is JavaScript, JavaScript two links down a nested chain, and one action referenced from two annotations (2 sites, 2 removed — different holders, so two references rather than one). A cyclic action graph is refused with UNSCANNABLE_ACTIONS rather than passed.",
      "arrayRule": "an array's meaning is decided by the key holding it: a destination under /OpenAction, /Dest or /D; a list of actions under /Next, every element of which is walked. An earlier version treated every array as a destination and stopped, so JavaScript inside a /Next array was never visited and the document passed.",
      "scannerScope": [
        "catalog /OpenAction",
        "catalog /AA",
        "catalog /Names /JavaScript, including /Kids",
        "page /AA",
        "annotation /A and every /AA sub-entry",
        "AcroForm fields walked through /Kids: /A and /AA",
        "/Next on every action found above"
      ],
      "boundedBy": "MAX_ACTION_DEPTH 32, with a per-chain cycle set; a chain deeper than the bound, a cycle, or an action dictionary that cannot be read makes the document UNSCANNABLE_ACTIONS rather than a quiet pass",
      "mechanismNote": "Two of the seven sites reach zero because copyPages never copies catalog-level structure, not because the sanitizer removed them. Recorded per site rather than summarised, so a future change that started copying /Names would not silently move a row from one column to the other.",
      "artifactWideRule": "The contract is two counts at zero, not one: what a reader can reach, and what the object table holds. Deleting the key that points at an action detaches it; it does not delete the object, and pdf-lib writes everything registered in the context whether or not anything points at it — the same mechanism that leaves orphaned pages behind after a copyPages. Measured: five of seven JavaScript cases put an action into the artifact's object table at all (annotation /A, annotation /AA, /Next, a doubly-nested /Next, and one indirect action referenced twice — 1 object each after the copy); the other two hold the action as a direct dictionary inside the annotation, so it never becomes a separate object. Every JavaScript action dictionary is now scrubbed of its entries and deleted from the table, and the artifact-wide count after reopening is 0 in all seven. Before this, 'no reachable JavaScript' was a true sentence about a file that still carried the script.",
      "recommended": "sanitize and prove by readback, refusing any document the scanner cannot finish inspecting; /OpenAction whose action is a page destination stays with M6-H5",
      "why": "Carrying document JavaScript into a derived document is a security decision nobody asked for, and refusing on mere presence would block documents where the script is incidental. What makes sanitization recommendable rather than hopeful is the readback count, and what makes it safe is that an uninspectable document is refused.",
      "defer": ["action types beyond JavaScript — /Launch, /ImportData and friends are not part of this envelope"]
    },
    {
      "id": "M6-H9d",
      "question": "Embedded files and attachments",
      "candidates": ["carry", "remove with explicit confirmation", "refuse"],
      "measured": "Dropped silently by both operations. pdf-lib has a write path for embedding files but no read path for copying a source's.",
      "detector": "catalog /Names /EmbeddedFiles present, or any /Filespec with an /EF",
      "recommended": "remove with explicit confirmation for Extract; carry for Merge is DEFER",
      "why": "An attachment belongs to the document, not to a page, so an extract of two pages has no principled claim to it — but losing one silently is the behaviour being corrected. The confirmation names the files being dropped.",
      "defer": ["carrying attachments through a Merge, where the union is at least well defined"]
    },
    {
      "id": "M6-H10",
      "question": "Merge intake — all-or-nothing or explicit partial",
      "candidates": ["all-or-nothing", "explicit partial with every omission named"],
      "measured": "Today a non-PDF is skipped by a bare continue and a failed load is caught and logged to the console; the file never appears in the list. broken-pagetree and malformed-acroform both LOAD cleanly, so intake cannot be 'whatever the loader accepts'.",
      "recommended": "explicit partial, with a per-input typed result and every omission named in the UI",
      "why": "Matches M5's adopted B2 batch shape. The page tree must be walked during intake, because two hostile fixtures load without complaint.",
      "defer": []
    },
    {
      "id": "M6-H11",
      "question": "Memory and output ceilings",
      "candidates": [
        "reuse M5's 512 MiB / 1 GiB / 2 GiB presets and MAX_OUTPUT_BYTES 268435456",
        "derive M6-specific ceilings",
        "adopt the output ceiling now and defer the memory budget to a sub-spike"
      ],
      "measured": "The dominant memory term — what a parsed pdf-lib document and its copied object graph weigh — is a function of document structure rather than byte length, and is UNKNOWN. The output ceiling needs no model: admitted at 3,462 B, refused at 3,461 B, at a boundary taken from a real artifact. No such check exists today.",
      "recommended": "adopt an actual-artifact output ceiling now, sharing M5's constant; do NOT adopt a memory preset until an Object-Graph Memory Sub-Spike closes the UNKNOWN",
      "why": "M5 RF-L2 forbade a hard budget containing an UNKNOWN that may materially exceed the ceiling. Reusing M5's presets for a differently shaped operation would be exactly that.",
      "defer": ["M6 Object-Graph Memory Sub-Spike — scope stated in budget.md"]
    },
    {
      "id": "M6-H12",
      "question": "Thumbnail strategy",
      "candidates": [
        "P1 — every page as a Data URL (current)",
        "P2 — windowed rendering",
        "P3 — bounded Blob URLs",
        "P4 — regenerated cache"
      ],
      "measured": "P1 retains 88,616 B of UTF-16 payload per A4 page and 538,997 B per A1 sheet (6.2x), 16.9 MiB for 200 A4 pages, 1,727 ms before anything is shown for 100 pages, with no ceiling anywhere. P2 at a 12-page window retains 1.0 MiB against P1's 8.4 MiB on the same source. P3 moves the image payload out of the JS heap: 0.4 MiB of Blob payload, with the URL strings and their JS objects still held.",
      "recommended": "P2 with P3's Blob URLs for the retained window, owned by the run",
      "basisNote": "Character counts and logical UTF-16 payload bytes are EXACT; what a JS engine allocates to hold a string, and the engine's own bookkeeping, are UNKNOWN. An earlier version of this research called characters × 2 'exact retained heap bytes', which it is not, and called P3 '0 B in the JS heap', which overstates it.",
      "boundTerms": [
        "maximum active thumbnail count",
        "maximum thumbnail pixel dimensions",
        "maximum retained Blob payload",
        "canvas pixel ceiling"
      ],
      "why": "The problem with P1 is not speed but that it has no bound: the cost is set by whatever document the user opens. The four terms above are quantities the product chooses and can enforce. No claim is made about total browser heap, which still contains UNKNOWN engine, PDF.js and decoded-image-cache terms.",
      "defer": ["P4 was not separately measured"]
    },
    {
      "id": "M6-H13",
      "question": "Ownership and cancellation",
      "candidates": ["generalize M5's RunOwnership", "a Split/Merge-specific contract"],
      "measured": "Two previews started together finish in the order second then first, so the slower first source overwrites the faster second one. There is no run token in the component. 5 exports created 5 object URLs and revoked 0. A PDF.js document opened for a preview is never destroyed.",
      "recommended": "generalize M5's RunOwnership, and extend it with preview-cache and object-URL cleanup on supersede",
      "why": "The invariant is identical — a superseded run leaves nothing behind — and two incompatible definitions of ownership across tools would be worse than one shared primitive. What M6 adds is that a superseded run must also release retained thumbnails and revoke URLs.",
      "defer": []
    },
    {
      "id": "M6-H14",
      "question": "Output naming",
      "candidates": ["keep extracted_<source>.pdf and merged_document.pdf", "derive a self-describing name"],
      "measured": "Every merge is named merged_document.pdf, so a second merge overwrites the first in the download folder; an extract says nothing about which pages it contains.",
      "recommended": "self-describing and deterministic: <source>_p1,3,5-8.pdf for Extract, merged_<n>files_<firstSource>.pdf for Merge",
      "why": "A download folder is shared state the product writes into; a fixed filename silently destroys the previous result.",
      "defer": ["length limits for very large selections"]
    }
  ]
}
