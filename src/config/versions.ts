/**
 * ツール別バージョン管理
 * 
 * セマンティックバージョニング: MAJOR.MINOR.PATCH
 * - PATCH (末尾): 小さな変更・バグ修正 (確認不要)
 * - MINOR (中間): 大きな機能追加・変更 (確認必要)
 * - MAJOR (先頭): 抜本的な見直し・破壊的変更 (確認必要)
 *
 * ユーザー向け機能をProductionへ出すPRでは、ここのバージョン/更新日に加えて
 * src/components/HowToUse.tsx の使い方ガイドと更新履歴 (releaseHistory) の更新も
 * Definition of Done に含める。実装だけ進んで説明が古いままにならないようにするため。
 */

export interface ToolVersion {
    version: string;
    lastUpdate: string; // YYYY/MM/DD形式
    changelog?: string; // 最新の変更内容
}

export const TOOL_VERSIONS: Record<string, ToolVersion> = {
    annotator: {
        version: '1.2.0',
        lastUpdate: '2026/09/08',
        changelog: '注釈付きPDFの保存を、元PDFのベクター・検索可能な文字情報を保持する方式へ変更'
    },
    comparator: {
        version: '1.1.1',
        lastUpdate: '2026/01/29',
        changelog: '初期リリース'
    },
    tools: {
        version: '1.3.1',
        lastUpdate: '2026/09/08',
        changelog: 'PDF表示処理に必要なPDF.js workerをアプリ同梱のlocal workerへ統一'
    },
    textifier: {
        version: '1.7.0',
        lastUpdate: '2026/09/07',
        changelog: '表題欄の位置を指定して、1ページ1行の図面一覧をExcel（.xlsx）へ書き出す機能を追加'
    },
    splitMerge: {
        version: '2.0.11',
        lastUpdate: '2026/09/24',
        changelog: '安全に処理できない場合は理由を示して中止するよう全面的に作り直し。'
            + '選択していないページが出力に混入する不具合を修正し、'
            + '統合時に読み込めなかったファイルを一覧に明示。'
            + 'ファイル名に抽出ページ・統合件数を含めるよう変更。'
            + '名前付きジャンプ先とレイヤー設定が無言で失われる不具合を修正し、'
            + '添付ファイルの削除確認を実際の添付の有無から判定するよう変更。'
            + '日本語や記号を含むフォーム名・入力値・ジャンプ先名・レイヤー名が崩れる不具合を修正し、'
            + '統合時の削除確認に添付ファイル名を表示するよう変更。'
            + '署名欄を含むPDFが抽出できない不具合を修正し、'
            + '読み取れない書式設定を無視せず理由を示して中止するよう変更。'
            + 'ストリーム長を参照で記録したPDFが書き出せない不具合を修正し、'
            + '図形単位でレイヤーを指定したPDFと表示の自動切替設定に対応。'
            + '同じ図形が複数の経路から参照されているPDFで、'
            + '非表示のレイヤーが書き出したPDFで表示されてしまう不具合を修正し、'
            + 'レイヤー一覧に登録されていないレイヤーは理由を示して中止するよう変更。'
            + '書き出したPDFのレイヤー設定に矛盾がないか、書き出し後に検査するよう追加。'
            + '添付ファイルやJavaScriptの目印を持つだけのレイヤー・図形・ページ内容が'
            + '削除されてしまう不具合を修正し、添付ファイルと確認できない構造は理由を示して中止するよう変更。'
            + '除去処理でレイヤー設定が変わる場合と、同じレイヤーが表示・非表示の両方に指定されている場合は'
            + '理由を示して中止するよう変更。'
            + 'JavaScriptの目印を持つだけの描画設定・図形グループ・ページ内容が削除され、'
            + '見た目が変わったまま書き出されてしまう不具合を修正。'
            + 'JavaScriptとして安全に取り除けない構造は、削除確認を表示する前に理由を示して中止するよう変更。'
            + 'リソース名が「A」などのアクション用のキーと同じというだけで、描画設定やプロパティが'
            + 'JavaScriptとして削除されてしまう不具合を修正し、アクションかどうかを保持している辞書の種類と'
            + '参照の位置から判定するよう変更。'
            + 'アクションの構造を最後まで読み取れないPDFは、抽出・統合とも削除確認より前に理由を示して中止し、'
            + '統合では該当ファイルだけを除外するよう変更。'
            + '統合では、存在しない参照を指す追加アクションなど、実行時にアクションを辿れないPDFを、'
            + '受け入れの時点で判定し、削除確認を表示する前に該当ファイルだけを除外するよう修正。'
            + 'アクションの連鎖を、定めた上限（32段）より少ない段数で打ち切り、'
            + '抽出・統合が途中で中止される不具合を修正。'
            + '次のアクションを配列で並べたPDFでも、1つずつ指定した場合と同じ上限（32段）まで読み取れるよう修正'
    }
};

// アプリ全体のバージョン（package.jsonと同期）
export const APP_VERSION = '1.1.1';
